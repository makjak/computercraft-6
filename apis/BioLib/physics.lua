local worldMeta = {
    function passSecond(self)
        for k, v in ipairs(self.points) do
            local pos = self.points[k].pos
            local x, y, z = unpack(pos)
            local motion = self.points
        end
    end
